# Superseded by environment.yml (conda). Use:
#   conda env create -f environment.yml
#   conda activate musclemap-data
#
# For a minimal pip-only supplement inside an existing OpenSim conda env, see
# the `pip:` section in environment.yml.
